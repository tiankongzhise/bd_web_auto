[project]
name = "auto-web"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "playwright>=1.50.0",
    "python-dotenv>=1.0.1",
    "tqdm>=4.67.1",
]
